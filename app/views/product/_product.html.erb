<% @products.each do |product| %>
    <div class="all_products">
      <div class="thumbnail">
        <%= link_to (image_tag product.image_path), product %>
        <div class="caption">
          <p><strong>Price:</strong> $<%= product.price %></p>
        </div>
      </div>
  </div>
<% end %>